# ========================================
# FIN ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# ========================================
# DATABASE CONFIGURATION
# ========================================

# Production Database
DATABASE_URL=jdbc:postgresql://localhost:5432/your_database_name
DATABASE_USER=your_database_username
DATABASE_PASSWORD=your_secure_database_password

# Test Database (optional)
TEST_DATABASE_URL=jdbc:postgresql://localhost:5432/your_test_database_name
TEST_DATABASE_USER=your_test_database_username
TEST_DATABASE_PASSWORD=your_test_database_password

# ========================================
# EMAIL CONFIGURATION (SMTP)
# ========================================

# SMTP Server Settings
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_USERNAME=your_email@yourdomain.com
SMTP_PASSWORD=your_secure_smtp_password
SMTP_AUTH=true
SMTP_TLS=false
SMTP_SSL=true

# Email Addresses
EMAIL_FROM=your_email@yourdomain.com
EMAIL_FROM_NAME="Your Company Name"
EMAIL_CONTACT=contact@yourdomain.com
EMAIL_PHONE="+27 XX XXX XXXX"

# IMAP Settings (for reading emails)
IMAP_HOST=mail.yourdomain.com
IMAP_PORT=993
IMAP_SSL=true

# ========================================
# APPLICATION SETTINGS
# ========================================

# Test Environment Flag
TEST_MODE=false

# License Auto-confirmation (for development)
FIN_LICENSE_AUTO_CONFIRM=true

# ========================================
# SECURITY NOTES
# ========================================
# - Change all default passwords
# - Use strong, unique passwords for each service
# - Consider using environment-specific .env files
# - Never commit this file or .env to version control
# - Store secrets securely (consider using Docker secrets or external secret management)
#
# JWT secret used to sign tokens. MUST be set in your environment before running Gradle or the app.
# Example (macOS/Linux):
#   export JWT_SECRET=your-local-jwt-secret && ./gradlew :app:bootRun
# Or source the .env file and export all vars:
#   set -a && source .env && set +a && ./gradlew :app:bootRun
#
# JWT_SECRET=your-jwt-secret-value

# GitHub token used by local scripts or CI to create PRs and perform merges. Use a
# Personal Access Token with the minimum scopes required (typically `repo` for full
# repo access or more limited scopes if available). Store securely and never commit
# the real token to the repository.
# Example (macOS/Linux):
#   export GITHUB_TOKEN=ghp_your_personal_access_token_here
#
# Option (example entry - commented out):
# GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

