-- Reconstructed V1 baseline schema
-- NOTE: This is a best-effort reconstruction created from the live database schema dump
-- The original migration file was lost; this migration is intentionally idempotent and will be a no-op
DO $$ BEGIN
    RAISE NOTICE 'Reconstructed migration V1 (baseline) - no schema changes applied. Review this file and replace with original SQL if available.';
END$$;
